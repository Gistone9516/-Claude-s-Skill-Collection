# 전역 설정 안내

`~/.claude` 폴더가 무엇으로 이루어져 있고 어떻게 굴러가는지 정리한 문서다. 사람이 읽는 문서라 한국어로 쓴다. AI가 읽는 파일(CLAUDE.md, 스킬 본문, 매니페스트)은 전부 영어다.

## 1. 구조

```
~/.claude/
  CLAUDE.md                  전역 규칙. 매 턴 자동 로드. 규칙 27개(G-01~G-27)
  manifest.json              이 설치본이 무엇을 갖춰야 하는지 선언한 단일 출처
  GUIDE.ko.md                이 문서
  README.md                  저장소 입구
  scripts/
    check-manifest.ps1       세션 시작마다 매니페스트와 실제 파일을 대조
    validate_doc.py          한국어 분량 실측용
  skills/                    스킬 20개(직접 만든 13개, 외부 7개)
  projects/<경로키>/memory/  폴더별 프로젝트 메모리
```

## 2. 세 층으로 나눈 이유

기준은 하나다. **지침이 행동하는 순간에 얼마나 가까이 있느냐.**

| 층 | 무엇이 들어가나 | 언제 작동하나 |
|---|---|---|
| 훅 (`scripts/guard.ps1`) | 기계적으로 탐지 가능한 규칙 4종 | 도구 호출 시점에 자동. 어길 수가 없다 |
| 스킬 | 그 작업을 할 때만 필요한 상세 수칙 | 해당 작업 착수 전에 전문 정독 |
| CLAUDE.md | 스킬을 읽기 전에 이미 참이어야 하는 것만 | 매 턴 자동. 대신 그만큼 희석된다 |

이 순서는 실측에서 나왔다. 2026년 7월 29일 정리 직전 CLAUDE.md는 17,848바이트까지 불어나 있었고, 규칙이 많아질수록 정작 중요한 것이 묻혔다. 더 결정적인 관찰은 그 다음이다. **스킬을 전문 정독하고도 같은 규칙(cp949 stdout)을 한 세션에서 두 번 어겼다.** 어느 파일에 적혀 있느냐가 아니라 행동과의 거리가 문제였다.

그래서 세 단계로 처리했다.

1. 기계적으로 잡히는 것은 훅으로 내렸다. 오탐 0을 만족하는 것만 골라 4개.
2. 나머지 상세 수칙은 스킬에 두고 작업 직전에 읽는다.
3. CLAUDE.md에 요약본으로 중복돼 있던 11개는 **지웠다**. 스킬에 더 자세히, 실측 사례까지 붙어서 들어 있는데 옆에 요약본을 두면 정확도만 떨어진다.

결과적으로 CLAUDE.md는 17,848에서 7,438바이트가 됐고, 규칙은 하나도 버려지지 않았다. 위치만 옮겨졌고 어디로 갔는지는 `manifest.json`의 `retiredGlobalRules`에 기록돼 있다.

### 훅이 강제하는 것

**결과를 보는 것** (`scripts/guard.ps1`)

| 검사 | 시점 | 규칙 |
|---|---|---|
| Write한 파일에 날 NUL 바이트 | PostToolUse (백그라운드) | SH-2 |
| `.bat`인데 LF 줄바꿈 | PostToolUse (백그라운드) | SH-15 |
| Bash 실패 출력에 `UnicodeEncodeError` | PostToolUseFailure | SH-12 |
| 파일 추가·삭제가 있는 커밋인데 README 미포함 | PostToolUse (git 명령만) | SH-23 |

**명령문을 보는 것** (`scripts/pattern-guard.sh`, 실행 **전**)

| 검사 | 규칙 | 실측 피해 |
|---|---|---|
| `Get-Content` → `Set-Content` 왕복 | SH-4 | README 전체 파괴 |
| `Remove-Item -Recurse` + SilentlyContinue 또는 node_modules | SH-5 | 731MB 잔존 |
| `EnumerateFiles` + `AllDirectories` | SH-8 | 드라이브 스캔 오보고 |
| 드라이브 루트나 시스템 위치를 `-Force` 없이 열거 | SH-9 | 16.4GB 페이지파일을 없다고 보고 |
| `Measure-Object -Line` | SH-10 | 764행을 480행으로 |
| `python -c`에 비ASCII | SH-1 | 런 정지 반복 |

명령문을 보는 쪽은 실행 **전**에 뜹니다. 명령문은 훅과 실행 사이에 바뀌지 않으므로 사전 판정이 정확하고, 저 여섯 개는 전부 파괴적이라 돌기 전에 멈추는 게 맞기 때문입니다.

**상기시키는 것** (`scripts/brief-nudge.sh`)

이번 세션에 한 번도 쓰지 않은 파일을 편집하려 하면, 편집 직전에 "브리핑 먼저 해보는 게 어떠냐"고 제안합니다. 위의 열 개와 성격이 다릅니다.

- **막지 않습니다.** 판정이 아니라 제안이고, 볼지 말지는 모델이 판단합니다. 고치려는 실패가 *반대*가 아니라 *망각*이기 때문입니다.
- **에이전트를 직접 띄우지 않습니다.** 스크립트 훅은 에이전트를 못 띄웁니다. 대신 상기시키면, 브리핑은 작업 맥락을 아는 쪽이 띄우게 됩니다.
- **브리핑은 두 대가 한 쌍입니다**(VF-21). 하나는 **현장** — 내가 손댈 파일이 실제로 뭐라고 적혀 있고 내 전제 중 뭐가 틀렸는지. 다른 하나는 **주변** — 이미 그 일을 하는 구현이 어디 있고, 뭐와 값이 같아야 하고, 내가 고치면 뭐가 낡는지. 같은 프롬프트로 두 대를 띄우면 같은 답이 두 번 옵니다(AI-4). 실제로 갈라놔야 두 번째가 값을 합니다.
- **세션별 원장**을 둡니다. 방금 만든 파일에까지 매번 뜨면 노이즈가 되고, 노이즈는 무시되기 때문입니다. 파일당 세션당 한 번만 뜹니다.

**선별 기준은 오탐 0이다.** 시끄러운 훅은 무시당하고, 무시당한 훅은 희석된 전역 규칙과 똑같은 실패다. 그래서 "복잡한 인라인 셸 금지"처럼 기준이 주관적인 것은 훅으로 만들지 않았다.

여기서 나온 설계 원칙이 하나 있다. **입력에서 예측하지 말고 결과에서 서명을 잡는다.** cp949는 명령문만 봐서는 데이터에 한글이 있는지 알 수 없어 사후에 에러 서명을 잡는다. README 검사도 원래 커밋 직전(PreToolUse)에 두었다가 첫 실전에서 오탐이 났다. `git add -A && git commit` 형태에서 훅이 `git add` 실행 **전에** 인덱스를 읽어, README가 들어 있는 커밋을 안 들어 있다고 보고했다. 훅이 관찰하는 상태를 관찰 대상 명령이 바꿔 버리면 구조적으로 정확할 수 없다. 그래서 실제로 만들어진 커밋을 사후에 읽는 쪽으로 옮겼다.

파일 검사 2종은 백그라운드로 돌아 문제가 없으면 세션에 비용이 0이다. 문제가 있을 때만 모델을 깨운다. git 검사는 `git`으로 시작하는 명령에만 붙는다(프로세스 하나가 600~700밀리초라 전체 Bash에 걸 수 없다).

**규칙에는 고정 ID가 붙어 있다.** 전역은 G-01부터, 스킬은 접두어별로 SH(셸), DG(진단), VF(검증), AU(자동화), WR(글쓰기), DC(문서), KT(킷), AO(위임), DF(deepflow), BF(buildflow), HX(hwpx)를 쓴다. 순서를 바꿔도 상호 참조가 깨지지 않고, 어느 규칙을 어겼는지 이름으로 지목할 수 있다.

## 3. 스킬 목록

### 작업 수칙 (해당 작업 착수 전 의무 정독)

| 스킬 | 언제 | 규칙 수 |
|---|---|---|
| `work-rules-shell` | 셸, git, 터미널, WSL 명령, 커밋, 폴더 이동 | 28 |
| `work-rules-diagnosis` | 원인 진단, 디버깅, 기능 제거, 코드 구현 및 리팩토링 착수 | 18 |
| `verify-fanout` | 남이 쓴 모듈 수정, 있다 없다 단정, 스펙 확정, 이식 코드 감사 | 22 |
| `work-rules-automation` | Workflow, 백그라운드 자동화, 장시간 멀티스텝 | 26 |
| `work-rules-writing` | 자소서, 보고서, 슬라이드, 주석 등 사람이 읽는 산출물과 디자인 | 14 |
| `work-rules-docs` | hwpx, pptx, docx, xlsx 편집과 텍스트 추출 | 14 |
| `work-rules-kit` | 재사용 킷, 프레임워크 공용 코드 | 4 |

전부 실제 사고에서 나온 규칙이다. 예를 들어 `work-rules-shell` SH-4는 PowerShell로 한글 파일을 한 줄 치환하려다 README 전체를 날린 사고에서 나왔다.

### `ai-characteristics` (규칙 14개) — 규칙이 왜 있는지

위 수칙들 중 상당수는 AI 자체의 성질을 고려해 만든 것이다. 그 근거를 한 곳에 모아 ID를 붙인 파일이다. 절차가 아니라 근거만 들어 있다.

예를 들어 이런 것들이다.

- `AI-1` 모델은 틀린 것을 확신을 갖고 말한다. 실측으로 100번 줄에 있는 필드를 93에서 99까지만 읽고 없다고 단정했다. 그래서 검증 규칙과 통짜 읽기 규칙이 있다.
- `AI-4` 같은 모델을 N개 붙여도 독립적인 판단이 아니다. 그래서 자문단의 합의는 약한 신호로만 취급한다.
- `AI-8` 완료 신호는 자기 보고지 검증이 아니다. 그래서 산출물을 디스크에서 직접 세고 실행으로 재검증한다.
- `AI-7` 근거 없는 규칙은 다음 세션이 개선하다가 없애 버린다. 그래서 규칙은 ID와 출처와 근거와 테스트를 함께 갖는다.

**규칙을 완화하거나 지우기 전에 이 파일부터 본다.** 해당 특징이 아직 유효하면 규칙도 유효하다. 규칙이 불필요해 보이는 것 자체가 근거가 안 보인다는 뜻이고, 그게 `AI-7`이 말하는 상황이다.

### 오케스트레이션 (에이전트를 쓸 때)

| 스킬 | 한 줄 정의 |
|---|---|
| `agent-ops` | 위임 방식의 단일 출처. 모델 분담, Workflow 오프로드, 보고 봉투, 팬아웃 상한 |
| `buildflow` | 기획에서 설계(계약)를 거쳐 병렬 구현까지 가는 멀티사이클 워크플로 |
| `deepflow` | 다각도 자문단, 그리고 비판과 개선을 반복하는 수렴 루프 |

셋의 관계는 단순하다. **`agent-ops`가 위임 정책의 정본**이고, 나머지 둘은 참조만 한다. 충돌하면 `agent-ops`가 이긴다. 셋 모두의 실행 기반은 스킬이 아니라 빌트인 `Workflow` 도구다.

**단, 메커니즘의 정본은 도구 설명 자체다.** `agent()`가 무엇을 받는지, 동시 실행 상한이 얼마인지, 어떤 함수가 예외를 던지는지는 스킬에 다시 적지 않고 도구 설명을 가리킨다. 스킬에는 정책(어느 단계에 어떤 모델과 effort를 줄지, 팬아웃을 누구 허락으로 할지, 하위 에이전트가 무엇을 돌려줘야 하는지)과 그 근거만 남긴다.

이렇게 나눈 이유는 실측 때문이다. 2026년 7월 29일에 `agent-ops`를 정리한 바로 다음 날 대조해 보니, 도구 설명과 어긋난 서술이 2건이고 새로 생긴 기능을 모르는 대목이 5건이었다. 도구 계약은 스킬보다 빨리 바뀌고, 베껴 적은 사실은 소리 없이 거짓이 된다.

어느 것을 쓸지는 이렇게 고른다.

```
사실 조회, 계산, 단일 정답인가
  예 -> 스킬 없이 바로 답한다
  아니오

코드나 기능을 여러 폴더에 구현하는가
  예 -> buildflow
  아니오

판단이 다각도로 갈리거나, 산출물을 수렴까지 반복 개선하는가
  예 -> deepflow (후자는 deepflow-auto)
  아니오

탐색이나 조사를 sonnet에 나눠 맡기는 일인가
  예 -> agent-ops만으로 충분
```

### 그 외

`new-hwpx-master`는 hwpx 공문 양식에 내용을 채우는 절차, `discord-bridge`와 `learning-harness`는 각각의 앱을 현재 폴더에 물리는 실행 안내다. `caveman` 계열 7개는 외부 제작물이고 매니페스트에 external로 등록만 해 뒀다.

## 4. 매니페스트와 이식

다른 컴퓨터로 옮겼을 때 스킬 하나가 빠져도 조용히 넘어가는 것이 문제였다. 규칙이 없는 채로 세션이 진행되는데 아무도 모른다.

`manifest.json`이 이 설치본이 갖춰야 할 것을 전부 선언하고, `scripts/check-manifest.ps1`이 세션 시작마다 실제 파일과 대조한다. 검사 항목은 여섯 가지다.

1. 선언된 스킬 폴더와 SKILL.md가 실제로 있는지
2. SKILL.md의 frontmatter 이름이 매니페스트와 일치하는지 (틀리면 스킬 호출이 안 된다)
3. 디스크에는 있는데 매니페스트에 없는 미등록 스킬
4. 필수 파일과 스크립트
5. CLAUDE.md가 크기 예산을 넘었는지 (다시 불어나는 것을 막는 장치)
6. 외부 앱 경로가 존재하는지

빠진 것이 있으면 세션 컨텍스트에 경고가 주입되고, Claude가 그 즉시 한국어로 알린다. 예를 들어 `work-rules-shell 스킬이 없습니다. 확인 바랍니다.` 라는 식이다. 필수 스킬이 빠졌으면 그냥 넘어가지 않고 이번 세션의 규칙이 불완전하다는 사실을 먼저 말하게 되어 있다.

수동으로 확인하려면 이렇게 돌린다.

```
powershell -NoProfile -File "C:\Users\USER\.claude\scripts\check-manifest.ps1" -Mode report
```

### 새 컴퓨터에 옮길 때

1. 저장소를 `~/.claude`로 clone 한다. 안 되면 CLAUDE.md, manifest.json, GUIDE.ko.md, skills/, scripts/ 만 복사해도 된다.
2. `settings.json`에 SessionStart 훅을 등록한다. 명령은 위 수동 실행과 같고 `-Event SessionStart`를 붙인다.
3. 세션을 하나 띄운다. 빠진 것이 있으면 검사기가 전부 알려 준다.
4. discord-bridge나 learning-harness를 쓸 거면 매니페스트의 `externalApps` 경로를 맞추거나 환경변수로 덮는다.

프로젝트 메모리 폴더 이름은 절대경로를 인코딩한 키라서 폴더 구조가 다른 컴퓨터로는 그대로 넘어가지 않는다. 정상이다.

## 5. 새 스킬을 추가할 때

1. `skills/<이름>/SKILL.md`를 만든다. frontmatter의 `name`은 폴더 이름과 같아야 한다.
2. `manifest.json`의 `skills` 배열에 같은 커밋으로 등록한다. 안 하면 다음 세션에서 미등록으로 잡힌다.
3. 트리거가 의무 정독 대상이면 `CLAUDE.md` §1 라우팅 표에 한 줄 추가한다.
4. 규칙에는 ID를 붙이고 파일 머리에 개수를 적는다. 개수가 안 맞으면 규칙이 소리 없이 추가되거나 사라진 것이다.

## 6. 스스로 학습한 교훈은 어디로 가나

반복 가능한 실수를 하면 그 자리에서 원인과 회피법까지 박제한다. 넣는 위치는 정해져 있다.

- 재사용 가능한 교훈은 해당 `work-rules-*` 스킬 본문에 실측 날짜와 함께
- 특정 프로젝트에만 해당하면 그 프로젝트 메모리에
- CLAUDE.md에는 넣지 않는다

넣고 나면 어디에 넣었는지 사용자에게 말한다.
